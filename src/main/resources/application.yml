
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/unit
    username: root
    password: admin


  jpa:
    hibernate:
            ddl-auto: update
#      ddl-auto: create-drop

    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: 'true'
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081